# Context Brief — Design alignment and real-data verification (2026-08-25)

- **Task:** Maintain the completed dark academic-operations alignment while closing only evidence-based real-data and release checks.
- **State:** Shared shell, private headers, status badges, form footers, Subject and Settings cards, public reader frame, and compact Reports/Attendance metric strips are checkpointed through `649f12d1`; `pnpm check`, `pnpm test` (17 tests), and `pnpm build` passed for the recent code increments.
- **In-flight files:** None. The working tree was clean after the `649f12d1` checkpoint.
- **Live state to restore:** Dev server is running on port 3000. Managed preview screenshots showed actual protected content despite intermittent historical `[Auth] Missing session cookie` log lines.
- **Next immediate step:** Verify the next available real retained record, media variant, or Resource link type without fabricating records; retain its tracker item if the needed example does not exist.
- **Gotchas:** Do not expose private Zoom inputs or owner data. Do not create test data solely to close verification tasks. A first History query failed on non-existent `h.summary`; the resolved error entry documents the correct `h.publicChangeSummary` field.
- **Related project files:** `todo.md`, `docs/design-system-alignment-brief.md`, `.project/logs/errors.md`, `.project/logs/progress.md`, and latest checkpoint `649f12d1`.
