# Context Brief — Full web app build (2026-08-24)

- **Task:** Build the complete Class Management System according to the approved unified execution plan, with Milestone 1 as the dark, Apple-inspired, mobile-first design source of truth.
- **State:** Foundation schema is reconciled in the database; owner/public server foundation, dark shell, Subject/Student/Schedule/No Class operations, Attendance basics, content server operations, content secretary screen, and published-only public content routes are implemented. `pnpm check` and `pnpm test` pass after the current segment. Repeated mobile/desktop checks confirmed `/app` and the public unavailable state render after the server settles; earlier blank/loading captures were premature.
- **In-flight files:** `server/routers/subjects.ts` provides Subject management. `server/routers/attendance.ts` provides Attendance and server-side `gpt-5-mini` advisory suggestions but needs parsing/normalization and a real secretary confirmation UI. `server/routers/content.ts` provides draft/create/publish operations. `client/src/pages/ContentPage.tsx` provides the secretary content screen. `client/src/pages/PublicPages.tsx` provides read-only shared content pages. `server/db.ts` has `getPublicContentItem`, which selects only published content under active, published Subjects. `server/routers.ts` registers all routes. Next immediate task is to provide Subject-level content entry links and remaining edit/archive/media/test work.
- **Live state to restore:** Dev server is managed and running on port 3000. No temp diagnostic scripts remain. The browser preview is not signed in.
- **Next immediate step:** Build Subject-level entry links and content editing/archive/media flows, then correct Zoom parsing/normalization/confirmation, and finally build reports/sharing/SEO and complete validation.
- **Gotchas:** Standing user approval covers routine logs/decisions. Do not create more planning layers. Do not expose raw Zoom text or suggestions publicly. Avoid fake reviews/testimonials. The schema migration originally partially applied; do not rerun the old migration wholesale.
- **Related project files:** `docs/final-web-app-execution-plan.md`, `docs/unified-milestone-build-plan.md`, `todo.md`, `.project/logs/errors.md`.
