import DashboardLayout from "@/components/DashboardLayout";
import { RecordStatusBadge } from "@/components/RecordStatusBadge";
import { WorkspacePageHeader } from "@/components/WorkspacePageHeader";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { downloadClassAttendancePdf, downloadCompiledAttendancePdf, downloadSubjectAttendancePdf } from "@/lib/reportPdf";
import { buildClassAttendanceCsv, buildClassAttendanceSummary, classAttendanceCsvFilename } from "@shared/attendanceCsv";
import { normalizedSubjectSelection } from "@shared/reportPdf";
import { Archive, ArchiveRestore, ChartNoAxesCombined, CheckCircle2, CircleDashed, Copy, Download, FileDown, FilePlus2, Send, SquareCheckBig, XCircle } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { useSearch } from "wouter";

export default function ReportsPage() {
  const search = useSearch();
  const sessionId = Number(new URLSearchParams(search).get("sessionId"));
  const utils = trpc.useUtils();
  const report = trpc.reports.allSubjectAttendance.useQuery();
  const classReport = trpc.reports.classAttendance.useQuery({ sessionId }, { enabled: Number.isInteger(sessionId) && sessionId > 0 });
  const reportList = trpc.reports.list.useQuery();
  const [selectedSubjectIds, setSelectedSubjectIds] = useState<number[]>([]);
  const [exporting, setExporting] = useState<string | null>(null);
  const refresh = () => utils.reports.list.invalidate();
  const create = trpc.reports.create.useMutation({ onSuccess: () => { refresh(); toast.success("Report saved as draft"); }, onError: error => toast.error(error.message) });
  const publish = trpc.reports.publish.useMutation({ onSuccess: () => { refresh(); toast.success("Report published"); }, onError: error => toast.error(error.message) });
  const archive = trpc.reports.archive.useMutation({ onSuccess: () => { refresh(); toast.success("Moved to Archive"); }, onError: error => toast.error(error.message) });
  const restore = trpc.reports.restore.useMutation({ onSuccess: () => { refresh(); toast.success("Restored as draft"); }, onError: error => toast.error(error.message) });
  const totals = (report.data ?? []).reduce((sum, item) => ({ present: sum.present + item.present, absent: sum.absent + item.absent, excused: sum.excused + item.excused, notSet: sum.notSet + item.notSet }), { present: 0, absent: 0, excused: 0, notSet: 0 });
  const selectedSubjects = useMemo(() => normalizedSubjectSelection(report.data ?? [], selectedSubjectIds), [report.data, selectedSubjectIds]);
  const busy = create.isPending || publish.isPending || archive.isPending || restore.isPending;

  useEffect(() => { const available = new Set((report.data ?? []).map(item => item.subjectId)); setSelectedSubjectIds(current => current.filter(subjectId => available.has(subjectId))); }, [report.data]);

  const exportClassAttendance = () => { if (!classReport.data) return; const file = new Blob([buildClassAttendanceCsv(classReport.data.students)], { type: "text/csv;charset=utf-8" }); const url = URL.createObjectURL(file); const download = document.createElement("a"); download.href = url; download.download = classAttendanceCsvFilename(classReport.data.subjectCode, new Date(classReport.data.startsAt)); document.body.appendChild(download); download.click(); download.remove(); URL.revokeObjectURL(url); toast.success("Private attendance CSV downloaded"); };
  const copyClassSummary = async () => { if (!classReport.data) return; try { await navigator.clipboard.writeText(buildClassAttendanceSummary({ subjectCode: classReport.data.subjectCode, subjectName: classReport.data.subjectName, startsAt: new Date(classReport.data.startsAt), present: classReport.data.present, absent: classReport.data.absent, excused: classReport.data.excused, notSet: classReport.data.notSet })); toast.success("Aggregate attendance summary copied"); } catch { toast.error("Could not copy the attendance summary"); } };
  const withPdfExport = async (key: string, action: () => Promise<void>) => { setExporting(key); try { await action(); toast.success("PDF downloaded"); } catch { toast.error("Could not create the PDF"); } finally { setExporting(null); } };
  const toggleSubject = (subjectId: number) => setSelectedSubjectIds(current => current.includes(subjectId) ? current.filter(id => id !== subjectId) : [...current, subjectId]);
  const selectAllSubjects = () => setSelectedSubjectIds(selectedSubjects.length === (report.data ?? []).length ? [] : (report.data ?? []).map(subject => subject.subjectId));

  return <DashboardLayout><section className="mx-auto max-w-5xl"><WorkspacePageHeader eyebrow="Reports" title="Reports" description="Download a session, a Subject, or selected Subjects." />
    {classReport.data ? <section className="signal-panel mt-6 border-t-2 border-primary p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="signal-kicker">Class Attendance</p><h2 className="signal-heading mt-2">{classReport.data.subjectName} · {classReport.data.subjectCode}</h2><p className="mt-1 text-sm text-muted-foreground">{new Date(classReport.data.startsAt).toLocaleString()}</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={copyClassSummary}><Copy className="mr-2 h-4 w-4" />Copy summary</Button><Button variant="outline" onClick={exportClassAttendance}><Download className="mr-2 h-4 w-4" />Download CSV</Button><Button variant="outline" disabled={exporting === "session"} onClick={() => withPdfExport("session", () => downloadClassAttendancePdf(classReport.data!))}><FileDown className="mr-2 h-4 w-4" />{exporting === "session" ? "Preparing PDF…" : "Download private PDF"}</Button><Button disabled={busy} onClick={() => create.mutate({ reportType: "class_attendance", subjectId: null, classSessionId: sessionId })}><FilePlus2 className="mr-2 h-4 w-4" />Create session report</Button></div></div><div className="mt-4 flex flex-wrap gap-2"><Badge variant="secondary" className="rounded-full text-emerald-300">{classReport.data.present} present</Badge><Badge variant="secondary" className="rounded-full text-red-300">{classReport.data.absent} absent</Badge><Badge variant="secondary" className="rounded-full text-sky-300">{classReport.data.excused} excused</Badge><Badge variant="secondary" className="rounded-full text-amber-300">{classReport.data.notSet} not set</Badge></div><section className="signal-inset mt-5 p-4"><div className="flex items-center justify-between gap-3"><h3 className="font-semibold">Official Student statuses</h3><Badge variant="secondary" className="rounded-full">Private</Badge></div><p className="mt-1 text-xs leading-5 text-muted-foreground">CSV and the private session PDF include Student names and official statuses only. Private excuse reasons and Zoom review data stay out.</p><div className="mt-4 max-h-72 divide-y divide-border overflow-auto pr-1">{classReport.data.students.map(student => <div key={student.canonicalName} className="flex items-center justify-between gap-3 py-3"><span className="min-w-0 truncate text-sm font-semibold">{student.canonicalName}</span><StatusBadge status={student.status} /></div>)}{!classReport.data.students.length ? <p className="py-4 text-center text-sm text-muted-foreground">No active Students are enrolled in this Subject.</p> : null}</div></section></section> : null}
    <div className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-4"><Metric icon={CheckCircle2} label="Present" value={totals.present} tone="text-emerald-300" /><Metric icon={XCircle} label="Absent" value={totals.absent} tone="text-red-300" /><Metric icon={FilePlus2} label="Excused" value={totals.excused} tone="text-sky-300" /><Metric icon={CircleDashed} label="Not set" value={totals.notSet} tone="text-amber-300" /></div>
    <section className="mt-7 py-4"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="signal-kicker">Across class desks</p><div className="mt-2 flex items-center gap-2"><ChartNoAxesCombined className="h-5 w-5 text-primary" /><h2 className="signal-heading">Subject Attendance</h2></div><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">Select the Subjects to include, then download one aggregate PDF. Individual Subject PDFs remain available on every list card.</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={selectAllSubjects} disabled={!report.data?.length}><SquareCheckBig className="mr-2 h-4 w-4" />{selectedSubjects.length === (report.data ?? []).length && report.data?.length ? "Clear selection" : "Select all"}</Button><Button disabled={!selectedSubjects.length || exporting === "compiled"} onClick={() => withPdfExport("compiled", () => downloadCompiledAttendancePdf(selectedSubjects))}><FileDown className="mr-2 h-4 w-4" />{exporting === "compiled" ? "Preparing PDF…" : `Download ${selectedSubjects.length || "selected"} Subjects`}</Button><Button variant="outline" disabled={busy} onClick={() => create.mutate({ reportType: "all_subject_attendance", subjectId: null, classSessionId: null })}><FilePlus2 className="mr-2 h-4 w-4" />Save all-Subject report</Button></div></div><div className="mt-5 space-y-3">{report.isLoading ? <p className="py-6 text-sm text-muted-foreground">Loading reports…</p> : null}{report.data?.map(item => { const selected = selectedSubjectIds.includes(item.subjectId); return <article key={item.subjectId} className={`signal-inset p-4 transition-colors ${selected ? "border-primary/50 bg-primary/5" : ""}`}><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><label className="flex min-w-0 cursor-pointer items-start gap-3"><input type="checkbox" checked={selected} onChange={() => toggleSubject(item.subjectId)} className="mt-1 size-4 accent-primary" aria-label={`Include ${item.subjectName} in compiled PDF`} /><span className="min-w-0"><span className="block font-semibold">{item.subjectName}</span><span className="mt-1 block text-xs text-muted-foreground">{item.subjectCode}</span></span></label><div className="flex flex-wrap items-center gap-2"><div className="flex flex-wrap gap-1.5 text-xs font-semibold"><Badge variant="secondary" className="rounded-full text-emerald-300">{item.present} present</Badge><Badge variant="secondary" className="rounded-full text-red-300">{item.absent} absent</Badge><Badge variant="secondary" className="rounded-full text-sky-300">{item.excused} excused</Badge><Badge variant="secondary" className="rounded-full text-amber-300">{item.notSet} not set</Badge></div><Button type="button" size="sm" variant="outline" disabled={exporting === `subject-${item.subjectId}`} onClick={() => withPdfExport(`subject-${item.subjectId}`, () => downloadSubjectAttendancePdf(item))}><FileDown className="mr-1.5 h-3.5 w-3.5" />{exporting === `subject-${item.subjectId}` ? "Preparing…" : "PDF"}</Button></div></div></article>; })}{!report.isLoading && !report.data?.length ? <p className="py-10 text-center text-sm text-muted-foreground">Add active Subjects and Attendance records to see the end-of-exams summary.</p> : null}</div></section>
    <section className="mt-7 pt-3"><p className="signal-kicker">Retained reports</p><h2 className="signal-heading mt-2">Saved reports</h2><div className="mt-4 divide-y divide-border">{reportList.data?.map(item => <ReportRow key={item.id} item={item} busy={busy} onPublish={() => publish.mutate({ id: item.id })} onArchive={() => archive.mutate({ id: item.id })} onRestore={() => restore.mutate({ id: item.id })} />)}{!reportList.data?.length ? <p className="py-8 text-center text-sm text-muted-foreground">No saved reports yet.</p> : null}</div></section>
  </section></DashboardLayout>;
}

function ReportRow({ item, busy, onPublish, onArchive, onRestore }: { item: { id: number; publicId: string; reportType: "class_attendance" | "all_subject_attendance"; publishState: "draft" | "published" | "archived"; version: number; subjectName: string | null; subjectCode: string | null; sessionStartsAt: Date | null; generatedAt: Date }; busy: boolean; onPublish: () => void; onArchive: () => void; onRestore: () => void }) {
  const copy = async () => { await navigator.clipboard.writeText(`${window.location.origin}/reports/${item.publicId}`); toast.success("Public report link copied for Messenger"); };
  const title = item.reportType === "class_attendance" ? "Class Attendance" : "All Subject Attendance";
  const detail = item.reportType === "class_attendance" ? `${item.subjectCode ?? "Subject"}${item.subjectName ? ` · ${item.subjectName}` : ""}${item.sessionStartsAt ? ` · ${new Date(item.sessionStartsAt).toLocaleString()}` : ""}` : `All active Subjects · Generated ${new Date(item.generatedAt).toLocaleDateString()}`;

  return <div className="signal-card-shell"><article className="signal-record-card p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold">{title}</h3><p className="mt-1 text-xs leading-5 text-muted-foreground">{detail}</p><p className="mt-2 text-xs text-muted-foreground">Version {item.version}</p></div><RecordStatusBadge tone={item.publishState === "published" ? "published" : item.publishState === "archived" ? "archived" : "draft"}>{item.publishState}</RecordStatusBadge></div><div className="mt-4 flex flex-wrap items-center gap-3">{item.publishState === "draft" ? <Button size="sm" disabled={busy} onClick={onPublish}><Send className="mr-1.5 h-3.5 w-3.5" />Publish</Button> : null}{item.publishState === "published" ? <><a href={`/reports/${item.publicId}`} target="_blank" rel="noreferrer" className="signal-action inline-flex min-h-11 items-center rounded-xl border border-border px-3 text-sm font-semibold text-foreground hover:bg-secondary">View shared</a><button onClick={copy} className="signal-action inline-flex min-h-11 items-center gap-1 px-2 text-sm font-semibold text-primary hover:bg-secondary"><Copy className="h-4 w-4" />Copy link</button><button onClick={onArchive} disabled={busy} className="signal-action inline-flex min-h-11 items-center gap-1 px-2 text-sm font-semibold text-muted-foreground hover:bg-secondary hover:text-foreground"><Archive className="h-4 w-4" />Archive</button></> : null}{item.publishState === "archived" ? <button onClick={onRestore} disabled={busy} className="signal-action inline-flex min-h-11 items-center gap-1 px-2 text-sm font-semibold text-primary hover:bg-secondary"><ArchiveRestore className="h-4 w-4" />Restore as draft</button> : null}</div></article></div>;
}
function StatusBadge({ status }: { status: "PRESENT" | "ABSENT" | "EXCUSED" | "NOT_SET" }) { const tone = status === "PRESENT" ? "text-emerald-300" : status === "ABSENT" ? "text-red-300" : status === "EXCUSED" ? "text-sky-300" : "text-amber-300"; return <Badge variant="secondary" className={`shrink-0 rounded-full text-xs ${tone}`}>{status === "NOT_SET" ? "Not set" : status[0] + status.slice(1).toLowerCase()}</Badge>; }
function Metric({ icon: Icon, label, value, tone }: { icon: typeof CheckCircle2; label: string; value: number; tone: string }) { return <section className="signal-inset p-3 sm:p-4"><Icon className={`h-4 w-4 ${tone}`} /><p className="mt-3 text-xs text-muted-foreground sm:mt-4 sm:text-sm">{label}</p><p className={`mt-1 font-[Manrope] text-2xl font-extrabold tracking-[-0.06em] ${tone} sm:text-3xl`}>{value}</p></section>; }
