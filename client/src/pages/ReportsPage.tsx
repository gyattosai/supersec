import DashboardLayout from "@/components/DashboardLayout";
import { RecordStatusBadge } from "@/components/RecordStatusBadge";
import { WorkspacePageHeader } from "@/components/WorkspacePageHeader";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { Archive, ArchiveRestore, ChartNoAxesCombined, CheckCircle2, CircleDashed, Copy, FilePlus2, Send, XCircle } from "lucide-react";
import { toast } from "sonner";
import { useSearch } from "wouter";

export default function ReportsPage() {
  const search = useSearch(); const sessionId = Number(new URLSearchParams(search).get("sessionId")); const utils = trpc.useUtils();
  const report = trpc.reports.allSubjectAttendance.useQuery(); const classReport = trpc.reports.classAttendance.useQuery({ sessionId }, { enabled: Number.isInteger(sessionId) && sessionId > 0 }); const reportList = trpc.reports.list.useQuery();
  const refresh = () => utils.reports.list.invalidate();
  const create = trpc.reports.create.useMutation({ onSuccess: () => { refresh(); toast.success("Report saved as draft"); }, onError: error => toast.error(error.message) }); const publish = trpc.reports.publish.useMutation({ onSuccess: () => { refresh(); toast.success("Report published"); }, onError: error => toast.error(error.message) }); const archive = trpc.reports.archive.useMutation({ onSuccess: () => { refresh(); toast.success("Moved to Archive"); }, onError: error => toast.error(error.message) }); const restore = trpc.reports.restore.useMutation({ onSuccess: () => { refresh(); toast.success("Restored as draft"); }, onError: error => toast.error(error.message) });
  const totals = (report.data ?? []).reduce((sum, item) => ({ present: sum.present + item.present, absent: sum.absent + item.absent, excused: sum.excused + item.excused, notSet: sum.notSet + item.notSet }), { present: 0, absent: 0, excused: 0, notSet: 0 });
  const busy = create.isPending || publish.isPending || archive.isPending || restore.isPending;
  return <DashboardLayout><section className="mx-auto max-w-5xl"><WorkspacePageHeader eyebrow="Secretary reports" title="Reports" description="Create a draft after each class session or for end-of-exams, then publish a shareable aggregate-only report." />{classReport.data ? <section className="signal-panel mt-6 border-t-2 border-primary p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="signal-kicker">Class Attendance</p><h2 className="signal-heading mt-2">{classReport.data.subjectName} · {classReport.data.subjectCode}</h2><p className="mt-1 text-sm text-muted-foreground">{new Date(classReport.data.startsAt).toLocaleString()}</p></div><Button disabled={busy} onClick={() => create.mutate({ reportType: "class_attendance", subjectId: null, classSessionId: sessionId })}><FilePlus2 className="mr-2 h-4 w-4" />Create session report</Button></div><div className="mt-4 flex flex-wrap gap-2"><Badge variant="secondary" className="rounded-full text-emerald-300">{classReport.data.present} present</Badge><Badge variant="secondary" className="rounded-full text-red-300">{classReport.data.absent} absent</Badge><Badge variant="secondary" className="rounded-full text-sky-300">{classReport.data.excused} excused</Badge><Badge variant="secondary" className="rounded-full text-amber-300">{classReport.data.notSet} not set</Badge></div><section className="signal-inset mt-5 p-4"><div className="flex items-center justify-between gap-3"><h3 className="font-semibold">Official Student statuses</h3><Badge variant="secondary" className="rounded-full">Private</Badge></div><p className="mt-1 text-xs leading-5 text-muted-foreground">This secretary-only roster detail is not included in the published aggregate report.</p><div className="mt-4 max-h-72 divide-y divide-border overflow-auto pr-1">{classReport.data.students.map(student => <div key={student.canonicalName} className="flex items-center justify-between gap-3 py-3"><span className="min-w-0 truncate text-sm font-semibold">{student.canonicalName}</span><StatusBadge status={student.status} /></div>)}{!classReport.data.students.length ? <p className="py-4 text-center text-sm text-muted-foreground">No active Students are enrolled in this Subject.</p> : null}</div></section></section> : null}<div className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-4"><Metric icon={CheckCircle2} label="Present" value={totals.present} tone="text-emerald-300" /><Metric icon={XCircle} label="Absent" value={totals.absent} tone="text-red-300" /><Metric icon={FilePlus2} label="Excused" value={totals.excused} tone="text-sky-300" /><Metric icon={CircleDashed} label="Not set" value={totals.notSet} tone="text-amber-300" /></div><section className="mt-7 border-y border-border py-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="signal-kicker">Across class desks</p><div className="mt-2 flex items-center gap-2"><ChartNoAxesCombined className="h-5 w-5 text-primary" /><h2 className="signal-heading">All Subject Attendance</h2></div></div><Button variant="outline" disabled={busy} onClick={() => create.mutate({ reportType: "all_subject_attendance", subjectId: null, classSessionId: null })}><FilePlus2 className="mr-2 h-4 w-4" />Create end-of-exams report</Button></div><div className="mt-5 divide-y divide-border border-y border-border">{report.isLoading ? <p className="py-6 text-sm text-muted-foreground">Loading reports…</p> : null}{report.data?.map(item => <div key={item.subjectId} className="flex flex-wrap items-center justify-between gap-3 py-4"><div><p className="font-semibold">{item.subjectName}</p><p className="mt-1 text-xs text-muted-foreground">{item.subjectCode}</p></div><div className="flex flex-wrap gap-2 text-xs font-semibold"><Badge variant="secondary" className="rounded-full text-emerald-300">{item.present} present</Badge><Badge variant="secondary" className="rounded-full text-red-300">{item.absent} absent</Badge><Badge variant="secondary" className="rounded-full text-sky-300">{item.excused} excused</Badge><Badge variant="secondary" className="rounded-full text-amber-300">{item.notSet} not set</Badge></div></div>)}{!report.isLoading && !report.data?.length ? <p className="py-10 text-center text-sm text-muted-foreground">Add active Subjects and Attendance records to see the end-of-exams summary.</p> : null}</div></section><section className="mt-7 border-t border-border pt-6"><p className="signal-kicker">Retained reports</p><h2 className="signal-heading mt-2">Saved reports</h2><div className="mt-4 divide-y divide-border border-y border-border">{reportList.data?.map(item => <ReportRow key={item.id} item={item} busy={busy} onPublish={() => publish.mutate({ id: item.id })} onArchive={() => archive.mutate({ id: item.id })} onRestore={() => restore.mutate({ id: item.id })} />)}{!reportList.data?.length ? <p className="py-8 text-center text-sm text-muted-foreground">No saved reports yet.</p> : null}</div></section></section></DashboardLayout>;
}

function ReportRow({ item, busy, onPublish, onArchive, onRestore }: { item: { id: number; publicId: string; reportType: "class_attendance" | "all_subject_attendance"; publishState: "draft" | "published" | "archived"; version: number; subjectName: string | null; subjectCode: string | null; sessionStartsAt: Date | null; generatedAt: Date }; busy: boolean; onPublish: () => void; onArchive: () => void; onRestore: () => void }) { const copy = async () => { await navigator.clipboard.writeText(`${window.location.origin}/reports/${item.publicId}`); toast.success("Public report link copied for Messenger"); }; const title = item.reportType === "class_attendance" ? "Class Attendance" : "All Subject Attendance"; const detail = item.reportType === "class_attendance" ? `${item.subjectCode ?? "Subject"}${item.subjectName ? ` · ${item.subjectName}` : ""}${item.sessionStartsAt ? ` · ${new Date(item.sessionStartsAt).toLocaleString()}` : ""}` : `All active Subjects · Generated ${new Date(item.generatedAt).toLocaleDateString()}`; return <article className="py-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold">{title}</h3><p className="mt-1 text-xs leading-5 text-muted-foreground">{detail}</p><p className="mt-1 text-xs text-muted-foreground">Version {item.version}</p></div><RecordStatusBadge tone={item.publishState === "published" ? "published" : item.publishState === "archived" ? "archived" : "draft"}>{item.publishState}</RecordStatusBadge></div><div className="mt-3 flex flex-wrap items-center gap-3">{item.publishState === "draft" ? <Button size="sm" disabled={busy} onClick={onPublish}><Send className="mr-1.5 h-3.5 w-3.5" />Publish</Button> : null}{item.publishState === "published" ? <><a href={`/reports/${item.publicId}`} target="_blank" rel="noreferrer" className="signal-action inline-flex min-h-11 items-center px-2 text-sm font-semibold text-primary hover:bg-secondary">View public</a><button onClick={copy} className="signal-action inline-flex min-h-11 items-center gap-1 px-2 text-sm font-semibold text-primary hover:bg-secondary"><Copy className="h-4 w-4" />Copy link</button><button onClick={onArchive} disabled={busy} className="signal-action inline-flex min-h-11 items-center gap-1 px-2 text-sm font-semibold text-muted-foreground hover:bg-secondary hover:text-foreground"><Archive className="h-4 w-4" />Archive</button></> : null}{item.publishState === "archived" ? <button onClick={onRestore} disabled={busy} className="signal-action inline-flex min-h-11 items-center gap-1 px-2 text-sm font-semibold text-primary hover:bg-secondary"><ArchiveRestore className="h-4 w-4" />Restore as draft</button> : null}</div></article>; }
function StatusBadge({ status }: { status: "PRESENT" | "ABSENT" | "EXCUSED" | "NOT_SET" }) { const tone = status === "PRESENT" ? "text-emerald-300" : status === "ABSENT" ? "text-red-300" : status === "EXCUSED" ? "text-sky-300" : "text-amber-300"; return <Badge variant="secondary" className={`shrink-0 rounded-full text-xs ${tone}`}>{status === "NOT_SET" ? "Not set" : status[0] + status.slice(1).toLowerCase()}</Badge>; }
function Metric({ icon: Icon, label, value, tone }: { icon: typeof CheckCircle2; label: string; value: number; tone: string }) { return <section className="signal-inset p-3 sm:p-4"><Icon className={`h-4 w-4 ${tone}`} /><p className="mt-3 text-xs text-muted-foreground sm:mt-4 sm:text-sm">{label}</p><p className={`mt-1 font-[Manrope] text-2xl font-extrabold tracking-[-0.06em] ${tone} sm:text-3xl`}>{value}</p></section>; }
